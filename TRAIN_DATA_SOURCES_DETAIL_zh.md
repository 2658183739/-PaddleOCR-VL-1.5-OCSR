# 训练数据来源、清洗与标注说明

本文档用于详细说明当前主训练线所使用的 7 部分数据分别来自哪里、为什么要这样组合、以及各部分数据在进入统一训练集之前经历了哪些清洗与标签规范化处理。本文档不试图把所有来源包装成“同一种数据”，而是强调它们在任务中的不同角色与边界。

## 一、主训练线的任务边界

当前主训练线的目标是单一的：给定一张分子结构图像，输出唯一的 `canonical SMILES`。因此，无论原始数据来自公开 benchmark、自建弱域样本还是合成补充样本，最终进入训练集时都必须满足两个条件：

1. 标签能够稳定落成唯一的 `canonical SMILES`；
2. 该标签与当前主任务的评测脚本兼容。

这意味着以下类型的标签不会直接进入主训练线：

- `ssml_normed`
- `chemfig`
- LaTeX 化学公式
- 其他与 `canonical SMILES` 不同口径的结构表示

因此，当前训练集不是“把所有可用化学图像都塞进去”，而是围绕主任务边界做过明确裁剪后的结果。

## 二、七部分数据的来源与角色

### 1. UOB

- 性质：公开 benchmark
- 任务角色：标准 printed OCSR 主分布
- 当前样本数：`5016`

`uob` 属于当前主训练集中的标准公开主分布来源之一。它的价值不在于覆盖真实世界噪声，而在于维持模型对常规 printed chemistry 图像的基本识别能力。对于一个需要在 benchmark 上可比较的 OCSR 模型而言，这类数据不能缺失，否则模型会在真实世界补强之后丢掉主基准分布的基础能力。

标签清洗策略：

1. 读取原始标签字段；
2. 统一规范为训练内部的 `canonical_smiles` 字段；
3. 使用 RDKit 做 canonicalization；
4. 无法成功 canonicalize 的样本不进入当前主训练线；
5. 最终转为统一的 `messages` 格式。

### 2. USPTO

- 性质：公开 benchmark
- 任务角色：标准 printed / patent-style 主分布
- 当前样本数：`5151`

`uspto` 和 `uob` 一起构成主训练线中的公开 benchmark 基座。与 `uob` 相比，`uspto` 更偏向专利/printed 风格，有助于维持模型在标准 OCSR benchmark 与专利样式结构图上的基础表现。

标签清洗策略与 `uob` 基本一致：

1. 统一读取结构标签；
2. 转为 `canonical_smiles`；
3. 通过 RDKit 做 canonicalization；
4. 清除空标签和无法解析标签；
5. 转为统一 `messages` 格式。

### 3. real_world

- 性质：项目组自建弱域补充层
- 任务角色：真实拍照、扫描、页面嵌入、试卷/讲义、手写等弱域补强
- 当前样本数：`4140`

`real_world` 不是单一官方 benchmark 名称，也不应写成某一个公开下载数据集。更准确的说法是：它是围绕 OCSR 真实应用场景，由项目组在多种结构图来源基础上构造的补充层，用于覆盖标准 benchmark 之外更容易掉分的场景。

当前这一层主要覆盖：

- `photo`
- `scan`
- `degraded_scan`
- `document_embed`
- `page_level`
- `chinese_exam`
- `handwritten`
- `journal_fig`
- `multi_grid`

从当前仓库与技术报告的证据链来看，`real_world` 更适合描述为 **project-built synthetic-realistic robustness layer**，而不是“完全由人工实拍构成的真实采集集”。也就是说，这一层并非全量纯真实采图，而是结合了公开结构来源的再组织、结构图嵌入、图像退化和真实页面风格模拟所形成的弱域补充层。

标注方案：

- 如果样本来自已知结构源的二次构造，则直接继承原结构标签；
- 若样本由公开结构图像裁切或页面嵌入生成，则沿用原分子的已知结构标签；
- 统一在进入主训练线前转为 `canonical_smiles`；
- 通过 RDKit 做 canonicalization；
- 不依赖从弱域图像本身重新“猜测”标签。

### 4. molgrapher_synthetic

- 性质：公开或可追溯的合成结构图来源
- 任务角色：复杂结构与视觉扰动补充
- 当前样本数：`4000`

这部分数据的作用不是提供真实场景，而是提供**复杂结构密度、结构拓扑复杂度与额外视觉扰动**。它的价值在于补足纯真实场景数据中不容易系统覆盖的长分子、复杂连接和布局变化。

标签清洗策略：

1. 保留结构标签；
2. 转为统一的 `canonical_smiles`；
3. 剔除非主任务标签格式；
4. 清除无法 canonicalize 的样本；
5. 与其他来源一起进入统一训练构造流程。

### 5. uspto30k_clean

- 性质：基于公开结构集合再组织得到的 clean 子集
- 任务角色：干净补充分布
- 当前样本数：`1500`

这部分的意义不是“多多益善”，而是提供一小部分整洁、规则、低噪声的结构图，用于在真实世界样本和复杂结构样本之外保留一个相对稳定的 clean reference distribution。

为什么不全量导入：

- 如果 clean 样本比例过高，模型很容易偏向渲染图风格；
- 这会削弱其对 `photo/scan/page_level` 等弱域的适应性。

因此它采用的是 **cap 1500** 的限额策略，而不是全量并入。

### 6. uspto30k_abbreviated

- 性质：基于公开结构集合再组织得到的 abbreviated 子集
- 任务角色：缩写/简写结构补充
- 当前样本数：`1500`

这部分主要用于补足缩写表达、局部结构简写、非完全展开式等更接近真实论文/专利图中的表达风格。它的作用和 `clean` 不同，不是给模型“更容易的样本”，而是给模型提供一种标准 benchmark 中比例不足的结构表达形式。

标签清洗方式与其他公开结构源一致：

- 转 `canonical_smiles`
- RDKit canonicalization
- 剔除无法稳定进入主任务标签空间的样本

### 7. uspto30k_large

- 性质：基于公开结构集合再组织得到的 large 子集
- 任务角色：大尺寸 / 长分子 / 稠密结构补充
- 当前样本数：`1500`

这部分的主要意义是把结构复杂度与图像复杂度同时推高。它对应的是：

- 更长的 SMILES
- 更大的图像尺寸
- 更复杂的局部布局

这类样本对于 OCSR 来说非常重要，因为它们往往是模型在真实环境中最容易失败的边角条件之一。但和 `clean` 一样，这部分也做了限额，不会无限放大，否则会让模型训练过程被少量极端长尾样本拖偏。

## 三、为什么当前是这七部分，而不是更多

当前这七部分不是“把现有数据都拼进去”，而是遵循了一个明确的分工逻辑：

- `uob/uspto`：负责主分布覆盖
- `real_world`：负责真实弱域补强
- `molgrapher_synthetic`：负责复杂结构与视觉扰动补充
- `uspto30k_clean/abbreviated/large`：负责从干净、简写、大尺寸三个角度补足标准公开结构集合的分层差异

这比单纯扩大样本总量更有效，因为它保留了训练集内部的结构性差异。

## 四、标签清洗方案

当前主训练线对公开数据的标签清洗遵循统一逻辑：

1. 读取原始结构标签；
2. 如果原始标签不是 `canonical_smiles`，先映射到可解释的结构串；
3. 统一进入 `canonical_smiles` 路径；
4. 使用 RDKit 做 canonicalization；
5. 清除空标签、无法解析标签和明显不在主任务边界内的标签；
6. 输出统一 `messages` 格式。

不进入主训练线的标签包括：

- `ssml_normed`
- `chemfig`
- LaTeX
- 其他不与主任务对齐的结构表示

## 五、自建/自构造数据的标注方案

当前主训练线中的 `real_world` 并不完全依赖人工从图像反标，而是尽量使用“已知结构 -> 派生图像”的方式保持标签可靠性。对于这类数据：

1. 先确定分子的结构标签；
2. 再进行图像构造、页面嵌入、退化或场景模拟；
3. 图像生成后直接继承该结构标签；
4. 最终统一转为 `canonical_smiles`。

这种方案的好处是：标注错误率显著低于“先拿复杂图，再人工推断结构”。

## 六、质量控制机制

当前训练集的质量控制是脚本化的，而不是仅依赖人工感觉。

审计项包括：

- 图像存在性检查
- 图像可读性检查
- prompt 正确性检查
- 空输出检查
- 非 SMILES 输出检查
- 与评测集的 ID/图片名/canonical SMILES 泄漏检查

当前审计结果显示：

- 缺失图片：`0`
- 不可读图片：`0`
- bad prompt：`0`
- 空输出：`0`
- 非 SMILES 输出：`0`
- 与 `v1p1` 的 ID 重叠：`0`
- 与 `v1p1` 的图片名重叠：`0`
- 与 `v1p1` 的 canonical SMILES 重叠：`0`

这说明主训练线在结构性质量控制层面已经形成闭环。

## 七、当前局限与下一步

虽然当前主训练线已经具备可复现的数据构建流程，但仍有几个明显短板：

1. `decimer / handwritten` 类样本在主训练线中仍不足；
2. 私有真实采集图仍偏少；
3. `edu_chemc` 当前更多用于评测与专项转换，而没有直接进入主训练线；
4. 真实拍照、扫描、页面嵌入与中文教育题面仍有进一步补强空间。

因此，下一阶段训练数据优化的重点不是“继续无差别增量”，而是围绕弱域做更明确的增补：

- `decimer/handwritten`
- `real_world/photo/scan`
- `edu_exam/chinese_exam`
- `page_level/document_embed`

## 八、如何把这一部分的评分拉高

如果目标是把“训练数据集构建科学性”显著拉高，最应该补的不是更多样本，而是把当前已经存在的流程写成可审计证据链：

1. 七部分来源清单
2. 每部分的来源性质（公开 / 项目自构造）
3. 统一标签清洗规则
4. 自构造样本的标注继承逻辑
5. 泄漏过滤逻辑
6. 审计结果
7. 统计分布表

把这七项讲清楚，评审就能看到这不是简单拼数据，而是一条有方法论的数据工程链。
