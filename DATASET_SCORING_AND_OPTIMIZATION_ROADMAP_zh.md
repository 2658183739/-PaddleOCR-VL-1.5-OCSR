# 数据集增分与后续优化路线图

本文档不是对当前结果的重复说明，而是面向后续提交与迭代的路线图。它重点回答三个问题：

1. 当前模型训练范式是什么，还能不能继续升级；
2. 数据集部分怎样从目前分数继续拉高；
3. 在不大幅增加训练成本的前提下，哪些优化最值得优先做。

## 一、当前是否只用了 SFT

是。当前主训练线本质上仍然是 **单阶段 LoRA SFT**。

具体来说：

- 基座模型：`PaddleOCR-VL-1.5`
- 微调方式：`LoRA SFT`
- 主输出空间：`canonical SMILES`
- 训练策略：真实世界样本上权、复杂合成样本适度补强、干净补充子集限额、评测集同分子过滤

从方法上看，当前项目还没有引入：

- DPO / ORPO / KTO
- RLHF / RLAIF
- 多模型 self-play
- 生成候选重排训练
- 结构级判别器联合训练

因此，当前阶段更准确的定位是：

> 一个面向 OCSR 主任务、以标签空间统一和弱域补强为核心的 LoRA SFT 工程基线。

## 二、能否用“agent 手法”加强

可以，但不建议一上来把它理解成“训练时上 agent”。对当前项目最有价值的 agent 化，不在主训练损失函数上，而在 **数据、评测和推理后处理** 三侧。

### 1. 数据侧 agent

数据侧可以引入的 agent 思路包括：

- 自动挖掘错误样本：
  - 把当前模型在 `real_world / decimer / edu` 上的高错样本自动归类；
  - 形成下一轮 weak-domain replay 候选池。

- 自动去重与泄漏检查：
  - 对训练/评测之间的 canonical SMILES 做交叉检查；
  - 对 EDU 候选池做唯一分子检查；
  - 对新增公开数据做统一过滤。

- 自动标注继承：
  - 对“已知结构 -> 页面嵌入 / 拍照 / 扫描 / 教学风格渲染”这类样本，不依赖重新人工标注；
  - 直接继承结构标签，再走 canonical 化和审计。

这类 agent 化的好处是：成本低、可控、不会破坏当前训练线的稳定性。

### 2. 评测侧 agent

评测侧可以做：

- 自动生成分来源报告；
- 自动输出抽检清单；
- 自动生成错误案例分桶；
- 自动对比 `base vs fine-tuned`；
- 自动输出“哪些来源提升最大 / 哪些来源仍然最差”。

这类改造对比赛加分的直接作用甚至大于再训一轮模型，因为它能显著提高：

- 文档质量
- 数据集质量说明
- 实验充分性

### 3. 推理侧 agent

推理侧最值得做的不是复杂 agent 框架，而是：

- 多候选生成
- RDKit 合法性筛选
- 基于合法率、长度、重复字符、结构可解析性的候选重排

这比直接上 RL 更省成本，也更符合当前 OCSR 场景的收益结构。

## 三、是否需要继续微调，以及怎么微调

需要，但优先顺序要明确。

当前最不划算的做法是：在数据说明和对比实验还没补全时，继续盲目重训很多轮。因为评委现在暴露出来的核心问题，更多在“数据科学性与说明不足”，而不是“模型完全没有结果”。

### 当前最值得做的微调升级

#### 1. 弱域 replay 继续加强

继续补：

- `decimer / handwritten`
- `real_world / photo / scan`
- `edu_exam / chinese_exam`
- `page_level / document_embed`

这是比改训练算法更直接有效的路线。

#### 2. 训练前做结构复杂度分层

当前最适合继续补的不是新的主分布，而是：

- 长分子
- 立体化学
- 多环结构
- 带电片段
- 缩写 / 局部简写

也就是说，下一轮训练集可以继续在现有 7 部分来源上做**结构复杂度再平衡**。

#### 3. 保持主任务边界，不要把标签空间重新放开

目前不建议为了“更多数据”重新把：

- `ssml_normed`
- `chemfig`
- LaTeX

直接并入主训练线。这样很可能会让模型再次偏离 `canonical SMILES` 输出约束。

### 当前暂不建议优先做的

- 直接从 SFT 跳到 RLHF
- 复杂 reward 设计
- 多阶段课程学习大改版
- 把所有评测层混成统一目标后再训练

这些都比当前最主要的问题更靠后。

## 四、怎样把数据集部分拉到 12 分以上

如果只看“训练数据集构建科学性”和“评估集质量”两个大项，当前最现实的增分方式不是继续堆样本，而是把现有流程写成可审计的证据链，并补几项低成本高收益材料。

### 1. 训练数据部分

当前最容易加分的是：

#### A. 七部分来源说明

把每部分都讲成“来源 + 角色 + 标签清洗方式”，而不是只给数量。

#### B. 数据构建流程图

建议补一张简单流程图：

```text
原始来源 -> 标签统一 -> canonical 化 -> 图像/路径检查 -> 泄漏过滤 -> 权重/限额 -> train_singleline_rw_messages.jsonl
```

#### C. 质检表

最少应单独放一张表：

- 缺失图片
- 不可读图片
- bad prompt
- 空输出
- 非 SMILES 输出
- 与评测集 ID 重叠
- 与评测集图片名重叠
- 与评测集 canonical SMILES 重叠

#### D. 统计表

最少单独列出：

- 来源分布
- 难度分布
- SMILES 长度分布
- 图像尺寸分布
- 图像长宽比分布

### 2. 评测集部分

当前最容易加分的是：

#### A. 来源链说明

明确写：

- `canonical_smiles_main_v1` 从哪来
- `canonical_smiles_curated_v2` 怎么清洗
- `ocsr_realworld_mixed_eval_v1` 怎么形成
- `ocsr_realworld_mixed_eval_v1p1` 怎么去重和回填

#### B. EDU 子集去重与回填证据

这部分很关键，因为它是评测集修改中最有“实质优化”的点。

建议明确展示：

- 去重前：153 图 / 97 唯一分子
- 去重后：153 图 / 153 唯一分子
- 去重移除：56
- 自动回填：56

#### C. 可视化抽检页

这是最推荐的低成本加分项。

建议：

- 每个来源抽 `10~20` 条
- 展示：
  - 图像
  - 标签
  - 来源
  - 难度
  - 备注

#### D. 质量控制边界说明

不要只写“基于规则做优化”，而要明确：

- 哪些是结构性 QC
- 哪些是标签检查
- 哪些是去重
- 哪些是转换失败丢弃
- 哪些是人工抽检建议

## 五、想让项目继续提分，最有效的优先级

### 第一优先级：补完整 baseline 对比

这是最直接提升说服力的动作。

- `PaddleOCR-VL-1.5 base`
- `fine-tuned merged model`
- `canonical_main`
- `mixed_v1p1`

完整四格对比能直接提升：

- 实验充分性
- 微调策略合理性
- 文档可信度

### 第二优先级：补抽检页和来源表

这会明显提升：

- 标注准确性
- 质检机制
- 数据统计分析

### 第三优先级：继续补弱域训练数据

重点仍然是：

- `decimer`
- `real_world`
- `edu_exam`

### 第四优先级：推理侧候选重排

如果不想马上开大训练，推理侧可以尝试：

- 多候选生成
- RDKit 合法性筛选
- 简单重排

这通常比直接改训练算法更省钱。

## 六、最终建议

当前阶段的最佳策略不是“重做模型”，而是：

1. 把数据来源、清洗、去重、回填、质检证据链全部写清楚；
2. 补齐 `base vs fine-tuned` 对比；
3. 补一个数据抽检页；
4. 然后再针对 `decimer / real_world / edu_exam` 做下一轮弱域补强。

如果做到这一步，数据集相关部分拿到 12 分以上是现实目标，而不是靠继续增加训练步数去“赌”。
