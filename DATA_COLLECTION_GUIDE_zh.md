# OCSR 数据集收集指南

## 先说结论

如果是第一次搞，不要一上来追求“很多来源”。最省力、性价比最高的路线是：

1. 用公开 SMILES 源挑一批分子。
2. 一条 SMILES 自动生成多种视图。
3. 少量再补真实手绘和真实拍照。

也就是说，**先做“同一个 SMILES 的多视图”**，再做“更多不同 SMILES”。

## 训练集和评测集怎么想

### 评测集

评测集最重要的是：

- 标签绝对可靠
- 风格代表真实难点
- 不进训练

所以评测集最适合用：

- 已知 SMILES
- 自己渲染/打印/拍照/扫描
- 少量高质量人工手绘

### 训练集

训练集更重要的是：

- 多样性
- 覆盖弱域
- 有一定噪声也没关系

所以训练集可以：

- 自动生成多视图
- 自动增强
- 加少量私有真实图
- 加公开手绘/文档类数据

## 一条 SMILES 一张图，还是多张图

### 评测集

推荐：

- 一条 SMILES 对应 `2-4` 张不同风格图

原因：

- 可以测鲁棒性
- 标签天然一致
- 更容易比较“同一结构在不同视觉条件下”的表现

推荐比例：

- `print_page`: 1
- `exam_page`: 1
- `photo_sim` 或真实手机拍照: 1
- `scan_sim` 或真实扫描: 1

也就是：

```text
100 条 SMILES -> 300-400 张评测图
```

### 训练集

推荐：

- 一条 SMILES 对应 `2-3` 张图为主
- 少量难样本可以到 `4-5` 张

原因：

- 太多同分子重复视图容易让训练集“水增多”
- 太少又学不到跨视图鲁棒性

## 第一次搞，最省事的采集方式

### 最推荐

#### 1. 电脑屏幕显示分子图，再用手机拍

优点：

- 最省事
- 自带屏幕摩尔纹、亮度不均、角度透视
- 很像真实用户随手拍屏

适合：

- `photo`
- `real_world_photo_scan`

#### 2. 渲染到 PDF / Word / PPT 页面，再用手机扫描 App 扫

优点：

- 非常适合补 `scan / degraded_scan / document_embed`
- 标签完全可控
- 可以快速做很多页

适合：

- `scan`
- `degraded_scan`
- `document_embed`

#### 3. 生成中文考试/讲义页面，再截图/拍照/扫描

优点：

- 很适合补 `edu_exam`
- 可以自动化批量生成
- 不需要你懂化学，只要有 SMILES

适合：

- `chinese_exam`
- `page_level`
- `document_embed`

#### 4. 少量真实手绘

优点：

- 对 `decimer` 弱域最有效
- 很难被纯增强替代

缺点：

- 人工成本最高

建议：

- 先做 `100-300` 张就够

## 比较独特、效果通常不错的方式

这些比“普通打印图”更值钱：

### 1. 手机拍电脑屏幕

很容易产生：

- 摩尔纹
- 反光
- 边缘裁切
- 轻微模糊

对模型很有锻炼价值。

### 2. 微信/QQ 发送图片再保存

会带来：

- 压缩
- 锐化异常
- 字体/线条损失

这类“社交软件二次压缩”很贴近真实使用场景。

### 3. 打印后折一下、压一下、再拍/扫

会带来：

- 阴影
- 纸张纹理
- 局部形变

对扫描/文档类鲁棒性很有帮助。

### 4. 把分子图嵌进题目、页眉、图注附近

比纯白底图更真实，尤其适合比赛答辩时讲“教育/文档复杂场景”。

## 推荐的视图比例

### 如果是做评测集

推荐一个很稳的起步方案：

```text
100 条 SMILES
  25% print_page
  25% exam_page
  25% photo_sim / 真实手机拍照
  25% scan_sim / 手机扫描
```

如果再加手绘：

```text
再额外加 20-40 条手绘
```

### 如果是做训练集

推荐起步：

```text
60% 自动生成/增强
20% 真实拍照/扫描
10% 中文考试风格
10% 手绘
```

如果人工很少，就把“真实拍照/扫描”和“手绘”优先保留，其他都让脚本自动做。

## 不懂化学，SMILES 该怎么挑

你不需要先懂结构式。只要会用公开数据库挑分子就够了。

### 最适合的来源

- PubChem
- ChEMBL

### 选择规则

优先挑：

- 长度分层
- 是否带立体化学
- 是否带卤素/杂原子
- 是否有环
- 是否有电荷

一个很实用的分层方案：

```text
30% 短分子:   8-25 个字符
40% 中分子:  26-60 个字符
20% 长分子:  61-100 个字符
10% 特殊分子: 含 @ / [N+] / Cl / Br / I / 多环 / 长链
```

### 看起来“值得选”的 SMILES 特征

- 有芳香环：`c1ccccc1`
- 有环编号：`1 2 3`
- 有卤素：`Cl Br F I`
- 有立体中心：`@`
- 有双键构型：`/` `\`
- 有带电原子：`[N+]` `[O-]`

## 我根本不知道分子长啥样，去哪儿看

最简单：

- PubChem 直接搜 SMILES 或名字，看结构图  
  官方入口：[PubChem Search](https://pubchem.ncbi.nlm.nih.gov/search/)
- PubChem 支持按 SMILES / InChI / 画结构搜索  
  说明页：[PubChem Quick Search](https://pubchem.ncbi.nlm.nih.gov/docs/quick-search)
- PubChem 结构搜索帮助  
  说明页：[PubChem Structure Search Help](https://pubchem.ncbi.nlm.nih.gov/search/help_search.html)

如果只是想快速“看看结构长什么样”，PubChem 已经够用了。

## 我建议你的最小行动

如果你现在想少干活，按这个来最值：

1. 从 PubChem/ChEMBL 抽 `100-200` 条 SMILES。
2. 用脚本自动生成 `print_page / exam_page / photo_sim / scan_sim`。
3. 再人工补：
   - `50-100` 张手机拍屏/拍打印纸
   - `50-100` 张手机扫描
   - `50-100` 张手绘

这样你几乎不用先懂化学，也能很快得到一套“有标签、种类多、解释得清楚”的数据集。
